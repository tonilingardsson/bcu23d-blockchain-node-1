{
  "name": "assignments",
  "version": "1.0.0",
  "description": "In the first assignment we have to create a node.js application that will imitate a Blockchain application. We can name it as we wish.",
  "main": "server.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=config/config.env server.mjs",
    "dev": "nodemon --env-file=config/config.env server.mjs",
    "node1": "nodemon --env-file=config/config.env server.mjs 5000 http://localhost:5000",
    "node2": "nodemon --env-file=config/config.env server.mjs 5001 http://localhost:5001",
    "node3": "nodemon --env-file=config/config.env server.mjs 5002 http://localhost:5002"
  },
  "keywords": [
    "Blockchain",
    "node.mjs",
    "application"
  ],
  "author": "Antonio Lingårdsson Luna",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@vitest/ui": "^1.5.3",
    "nodemon": "^3.1.0",
    "vitest": "^1.5.3"
  }
}