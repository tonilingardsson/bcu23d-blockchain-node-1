{
  "name": "assignments",
  "version": "1.0.0",
  "description": "In the first assignment we have to create a node.js application that will imitate a Blockchain application. We can name it as we wish.",
  "main": "server.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=production node server.mjs",
    "dev": "nodemon server"
  },
  "keywords": [
    "Blockchain",
    "node.js",
    "application"
  ],
  "author": "Antonio Lingårdsson Luna",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}